window.__APP_CONFIG__ = {
  APP_SURFACE: "${APP_SURFACE}",
  APP_BRAND: "${APP_BRAND}",
  API_BASE_URL: "${API_BASE_URL}",
  APP_PUBLIC_BASE_URL: "${APP_PUBLIC_BASE_URL}",
  PRIMARY_LANDING_URL: "${PRIMARY_LANDING_URL}"
};
